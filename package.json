{
  "name": "em-hrms",
  "private": true,
  "workspaces": [
    "apps/foundations/*",
    "apps/hiring/*",
    "apps/onboarding/*",
    "apps/admin-dashboard"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
