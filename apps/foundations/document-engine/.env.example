DATABASE_URL="postgresql://em_hrms:em_hrms_local@localhost:5432/em_hrms_document_engine"
PORT=4001
# Simple shared-secret auth for internal service-to-service calls (see docs/contracts/document-engine-api.md)
INTERNAL_API_KEY="dev-local-shared-secret"
# Set to "keka" once Keka's eSign API entitlement is confirmed; otherwise standalone stub is used.
ESIGN_PROVIDER="standalone"
