# Postgres connection for this app's own database (one DB per leaf app).
DATABASE_URL="postgresql://em_hrms:em_hrms_local@localhost:5432/em_hrms_bgv_tracking?schema=public"

# Port the Express server listens on.
PORT=4002
