# Postgres connection for this app's own database (one DB per leaf app — see docs/PROGRAM-OVERVIEW.md)
DATABASE_URL="postgresql://em_hrms:em_hrms_local@localhost:5432/em_hrms_induction_portal?schema=public"

# Port this service listens on
PORT=4001

# Path to the content directory (markdown/JSON) HR edits without a redeploy
CONTENT_DIR=./content
